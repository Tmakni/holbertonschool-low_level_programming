#include <stdio.h>
#include <stdlib.h>
char *create_array(unsigned int size, char c)
{
	int *str;
	int i = 0;

	while (str[i] != '\0')
	{
		i++;
	}
	return (i);
	
	str = malloc(sizeof (char) * (*create_array));
	if (size = 0)
	{
		return (0);
	}
	free (str);
	return (0);
}
